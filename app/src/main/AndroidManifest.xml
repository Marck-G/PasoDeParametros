<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prom.dev.mk.pasodeparametros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Act1" android:label="Nombre y Apellidos" />
        <activity android:name=".Act1b"  android:label="Aceptar Condiciones"/>
        <activity android:name=".Act2" android:label="Calculadora Simple" />
        <activity android:name=".Act2b" android:label="Resultado Operación"/>
        <activity android:name=".Act3" android:label="Solicitud de Datos" />
        <activity android:name=".Act3b" android:label="Muestra Datos"></activity>
    </application>

</manifest>